#version 330 core

in vec3 outColor;
in vec2 UV;

// Output data
out vec4 color;

uniform sampler2D textureSampler;

void main() {

	// Output color = red 
	//color = vec3(1,0,0);
	color = vec4(outColor, 1.0f);
	//color = texture(textureSampler, UV).rgb;
}
